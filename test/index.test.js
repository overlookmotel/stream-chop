/* --------------------
 * stream-chop module
 * Tests
 * ------------------*/

'use strict';

// Modules
const streamChop = require('../index');

// Init
require('./support');

// Tests

describe('tests', () => {
	it.skip('all', () => { // eslint-disable-line jest/no-disabled-tests
		expect(streamChop).not.toBeUndefined();
	});
});
